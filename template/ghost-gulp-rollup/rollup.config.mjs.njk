// SPDX-FileCopyrightText: 2022 Johannes Loher
// SPDX-FileCopyrightText: 2022 David Archibald
//
// SPDX-License-Identifier: MIT

{% if useTypeScript %}import typescript from '@rollup/plugin-typescript';
{% endif %}import { nodeResolve } from '@rollup/plugin-node-resolve';

export default () => ({
  input: 'src/module/{{ packageId }}.{% if useTypeScript %}ts{% else %}js{% endif %}',
  output: {
    dir: 'dist/module',
    format: 'es',
    sourcemap: true,
  },
  plugins: [nodeResolve(){% if useTypeScript %}, typescript(){% endif %}],
});
