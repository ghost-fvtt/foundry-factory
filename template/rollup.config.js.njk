{% if typescript %}const typescript = require('rollup-plugin-typescript2');
{% endif %}const { nodeResolve } = require('@rollup/plugin-node-resolve');

module.exports = {
    input: 'src/module/{{ entryPoint }}',
    output: {
        dir: 'dist/module',
        format: 'es',
        sourcemap: true,
    },
    plugins: [nodeResolve(){% if typescript %}, typescript({}){% endif %}],
};
