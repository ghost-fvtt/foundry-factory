module.exports = {
{% if useTypeScript %}  parser: '@typescript-eslint/parser',

  parserOptions: {
    ecmaVersion: 2020,
    sourceType: 'module',
  },

{% endif %}  extends: [{% if useTypeScript %}'plugin:@typescript-eslint/recommended'{% else %}'eslint:recommended'{% endif %}, 'plugin:prettier/recommended'],
{% if useTypeScript %}
  plugins: ['@typescript-eslint'],
{% endif %}
  rules: {
    // Specify any specific ESLint rules.
  },
{% if useTypeScript %}
  overrides: [
    {
      files: ['./*.js'],
      rules: {
        '@typescript-eslint/no-var-requires': 'off',
      },
    },
  ],
{% endif %}};
